services:
  # Backend Service
  - type: web
    name: wesm-prediction-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --timeout 120 --workers 1 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

  # Frontend Service (optional - or use Vercel)
  - type: web
    name: wesm-prediction-frontend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    envVars:
      - key: VITE_API_URL
        value: https://wesm-prediction-api.onrender.com
```

## **Solution 3: Quick Fix - Manual Override**

### **In Render Dashboard:**

1. Go to your service settings
2. **Environment:** Change from `Node` to `Python 3`
3. **Build Command:** `pip install -r requirements.txt`
4. **Start Command:** `gunicorn --bind 0.0.0.0:$PORT --timeout 120 --workers 1 app:app`
5. Click **"Save Changes"**
6. **Manual Deploy** → **"Deploy latest commit"**

## **Recommended Project Structure (Clean Separation):**
```
your-project/
│
├── backend/              # Deploy to Render
│   ├── app.py
│   ├── requirements.txt
│   ├── lightgbm_modelver2.pkl
│   └── .gitignore
│
└── frontend/             # Deploy to Vercel
    ├── src/
    ├── package.json
    ├── vite.config.js
    └── .gitignore
